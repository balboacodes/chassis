/**
 * Inject dependencies into a class constructor or method based on its parameter types.
 */
// @ts-expect-error
export default function inject(target: any, property?: string, descriptor?: PropertyDescriptor): void {}
