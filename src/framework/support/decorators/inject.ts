/**
 * Inject dependencies into a class constructor or method based on its parameter types. Only works for controllers and
 * service provider constructors and boot methods.
 */
// @ts-expect-error
export default function inject(target: any, property?: string, descriptor?: PropertyDescriptor): void {}
